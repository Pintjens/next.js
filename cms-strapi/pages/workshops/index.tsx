import { log } from "console";

export default function HealingOverview() {
  
  // const workshops : [Workshop] = await getStrapiData("workshops?populate=*");
  // log(workshops)
  return(
    <main>
        <h1>Workshops</h1>

        <h2>Under costruction, check back later</h2>
          


        {/* {workshops.map(workshop => 
          <a href={`/workshops/${workshop.slug}`}>
            <article>
              <h3>{workshop.name}</h3>
            </article>
          </a>
        )} */}
      </main>
    )
  }
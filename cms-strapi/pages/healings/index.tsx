
import { log } from "console";


export default function HealingOverview() {
  
  // const healings : [Healing] = await getStrapiData("healings?populate=*");
  // log(healings);
  return(
    <main>
        <h1>Healings</h1>

        <h2>Under costruction, check back later</h2>
          
        {/* {healings.map(healing => 
          <a href={`/healings/${healing.slug}`}>
            <article>
              <h3>{healing.name}</h3>

            </article>
          </a>
        )} */}
      </main>
    )
  }